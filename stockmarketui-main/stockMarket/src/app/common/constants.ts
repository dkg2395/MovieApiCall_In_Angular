import { environment } from "src/environments/environment";

export class Constants {
    static Company_BASE_URL:string = environment.companyBaseApiUrl;
    static Stock_BASE_URL:string = environment.stockBaseApiUrl;
}
